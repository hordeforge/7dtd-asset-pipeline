name: ci

on:
  push:
    branches: [main]
  pull_request:

# One run per ref: a fresh push cancels the superseded in-progress run
# instead of queueing duplicate parallel jobs.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least privilege: the pipeline only needs to read the code.
permissions:
  contents: read

# The implicit shell is `bash -e`, which has no pipefail, so a failing command
# on the left of a pipe is reported as the exit code of the one on the right:
# `shamway build | tee log` passes on a failed build. Naming bash explicitly
# gets `bash --noprofile --norc -eo pipefail`.
defaults:
  run:
    shell: bash

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      # Actions pinned by full commit SHA (tag in comment) so a moved tag or a
      # compromised major-version release cannot inject code into the pipeline.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      - run: uv python install ${{ matrix.python-version }}
      # Install the core alone: the suite must pass without the optional
      # capabilities, because that is how most consumers first run it.
      # `uv sync --locked` resolves from the committed uv.lock and verifies
      # its hashes — never a fresh resolution against PyPI — and brings the
      # dev group (ruff, mypy, setuptools) that `make check` needs.
      - run: uv sync --locked --python ${{ matrix.python-version }}
      # Later steps (and the Makefile's `uv run`) resolve ./.venv from PATH.
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      # Exercise the installed console script, not only the source tree, so a
      # broken entry point or missing package data fails here rather than in a
      # consumer's checkout.
      - run: shamway --help
      - run: shamway schema
      - run: make check test

  macos:
    # docs/getting-started/quickstart.md claims the CLI itself is portable beyond Linux; this
    # job is that claim's only mechanical evidence, and it gates. Lint,
    # typecheck, and the shell-script gates stay on Linux: they pin analyzer
    # versions once, and shellcheck is not part of the macOS runner image. One
    # Python version, because the Linux matrix already covers the supported
    # range.
    #
    # The suite is filesystem-honest rather than Linux-shaped: the two
    # stem-collision tests skip on a case-insensitive volume (the runner's
    # default APFS cannot hold two casings of one name, so the scenario does
    # not exist there), and path assertions compare resolved spellings so the
    # /var -> /private/var symlink does not read as a product difference.
    runs-on: macos-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      - run: uv python install 3.13
      - run: uv sync --locked --python 3.13
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      - run: shamway --help
      - run: shamway schema
      - run: uv run --no-project python -m unittest discover -s tests
        env:
          PYTHONPATH: src

  capabilities:
    # The optional capabilities must also work, and must not be required.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      - run: ./scripts/bootstrap
      - run: .venv/bin/shamway capabilities --json
      - run: .venv/bin/shamway capabilities --missing
      # make check resolves both analyzers by name; bootstrap's uv sync
      # installed the pinned dev group into the same venv.
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      - run: make check test

  scaffold:
    # Prove the consumer path end to end on a runner that has never had a Unity
    # editor: a bare modlet is scaffolded, an asset is authored into it, a real
    # bundle is written, and every XML reference to it validates. This job is
    # the gate on the claim that Unity is opt-in — if the default path ever
    # needs an editor again, nothing else here would notice.
    #
    # It gates the shader lane in both states, because the runner makes the
    # awkward one free: Ubuntu packages vkd3d-compiler 1.2, which predates the
    # HLSL support this writer needs (added in vkd3d 1.3), so the packaged
    # binary exercises the degraded path and a source build exercises the whole
    # one.
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      # Pinned so a vkd3d release cannot change what this job proves. Matches
      # the version the writer was developed against.
      VKD3D_VERSION: "1.19"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      # The optional extras carry trimesh (reads the mesh) and Pillow (writes
      # the texture). Still no editor, no game install, and no network at run
      # time.
      - run: uv sync --locked --no-dev --all-extras
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"

      # RUNNER_TEMP rather than /tmp: it is disk-backed and cleaned per job,
      # and it is where this repository's own scripts point TMPDIR. Exported
      # through GITHUB_ENV because the `runner` context is not available in a
      # job-level `env:` block, only inside a step.
      - run: echo "MOD_DIR=$RUNNER_TEMP/MyMod" >> "$GITHUB_ENV"

      - name: Scaffold a modlet with no flags, and prove the default is editorless
        run: |
          mkdir -p "$MOD_DIR/Config"
          printf '<xml><Name value="MyMod" /></xml>\n' > "$MOD_DIR/ModInfo.xml"
          shamway init "$MOD_DIR" --unity-version 2022.3.62f2
          test -f "$MOD_DIR/tools/shamway/AGENTS.md"
          test -d "$MOD_DIR/assets-src/bundle"
          # No editor was requested, so none of its scaffolding may appear.
          test ! -e "$MOD_DIR/tools/shamway/UnityProject"
          grep -q 'bundle_source = "synthesized"' "$MOD_DIR/.shamway.toml"
          cd "$MOD_DIR"
          shamway status --json || true
          shamway refs
          shamway call capabilities > /dev/null

      - name: Author the sources the writer reads
        run: |
          # The fixtures are a sibling script, so this workflow stays one
          # language and `make check` lints and type-checks the authoring.
          python "$GITHUB_WORKSPACE/scripts/ci_scaffold.py" author-sources \
            "$MOD_DIR/assets-src/bundle"
          cd "$MOD_DIR"
          printf '%s\n' \
            '<configs><append xpath="/blocks"><block name="myModBlock">' \
            '<property name="Model" value="#@modfolder(MyMod):Resources/mymod.unity3d?myModThing.prefab" />' \
            '</block></append></configs>' > Config/blocks.xml

      # ---- the shader lane, short: Ubuntu's own vkd3d-compiler is too old ----
      - name: Install the packaged (too old) vkd3d-compiler
        run: sudo apt-get update -qq && sudo apt-get install -y --no-install-recommends vkd3d-compiler
      - name: A too-old vkd3d must degrade loudly, never fail half-way
        env:
          UNITY_EDITOR: ""
        run: |
          check="$GITHUB_WORKSPACE/scripts/ci_scaffold.py"
          cd "$MOD_DIR"
          vkd3d-compiler --version || true
          shamway capabilities --json > "$RUNNER_TEMP/caps-old.json"
          python "$check" check-vkd3d "$RUNNER_TEMP/caps-old.json" --expect unusable
          # The build must still succeed, packing less and saying so.
          shamway build | tee "$RUNNER_TEMP/degraded.log"
          grep -q "cannot be used" "$RUNNER_TEMP/degraded.log"
          shamway validate
          shamway inspect --deep --json Resources/mymod.unity3d > "$RUNNER_TEMP/degraded.json"
          # The prefab group must be absent: packing a prefab with no usable
          # compiler ships something that loads and draws nothing.
          python "$check" check-classes "$RUNNER_TEMP/degraded.json" \
            --require Mesh Texture2D --forbid GameObject Material Shader

      # ---- the shader lane, whole: a vkd3d new enough to read HLSL ----
      - name: Cache the vkd3d build
        id: vkd3d-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: /opt/vkd3d
          key: vkd3d-${{ env.VKD3D_VERSION }}-${{ runner.os }}
      # Deliberately the documented route, not a copy of it: this is the exact
      # command Debian and Ubuntu users are told to run, so CI gates the recipe
      # rather than something that merely resembles it.
      - name: Build vkd3d-compiler from source, the way the docs say to
        if: steps.vkd3d-cache.outputs.cache-hit != 'true'
        env:
          VKD3D_SOURCE_VERSION: ${{ env.VKD3D_VERSION }}
          VKD3D_SOURCE_PREFIX: /opt/vkd3d
        run: scripts/install-tools.sh --with-vkd3d-source
      - name: The whole lane must produce every class the game resolves
        env:
          UNITY_EDITOR: ""
        run: |
          echo "/opt/vkd3d/bin" >> "$GITHUB_PATH"
          export PATH="/opt/vkd3d/bin:$PATH"
          export LD_LIBRARY_PATH="/opt/vkd3d/lib:${LD_LIBRARY_PATH:-}"
          vkd3d-compiler --version
          check="$GITHUB_WORKSPACE/scripts/ci_scaffold.py"
          cd "$MOD_DIR"
          shamway capabilities --json > "$RUNNER_TEMP/caps-new.json"
          python "$check" check-vkd3d "$RUNNER_TEMP/caps-new.json" --expect usable
          shamway build
          shamway validate
          shamway inspect --deep --json Resources/mymod.unity3d > "$RUNNER_TEMP/deep.json"
          # Every class the game resolves, written with no editor.
          python "$check" check-classes "$RUNNER_TEMP/deep.json" \
            --require AssetBundle GameObject Transform MeshFilter MeshRenderer \
                      Mesh Material Shader Texture2D

  coverage-badge:
    # Publishes the line-coverage badge to the badges branch, which
    # raw.githubusercontent serves to the README. Main pushes only: a PR gets
    # its verdict from the jobs above and never sees the write token this job
    # needs.
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      # Actions pinned by full commit SHA (tag in comment) so a moved tag or a
      # compromised major-version release cannot inject code into the pipeline.
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          enable-cache: true
      # Core only, no dev tools: coverage runs the unit suite directly.
      - run: uv sync --locked --no-dev
      - run: echo "$PWD/.venv/bin" >> "$GITHUB_PATH"
      - run: make coverage
      - run: uv run --no-project --with coverage python scripts/coverage_badge.py "$RUNNER_TEMP/coverage.svg"
      - name: Publish the badge to the badges branch
        env:
          # The default token is not in the environment unless mapped here,
          # and set -u fails the step on the unbound GITHUB_TOKEN otherwise.
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          url="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          if git ls-remote --exit-code --heads origin badges >/dev/null 2>&1; then
            git clone --depth 1 --branch badges "$url" badge-repo
          else
            git init -q -b badges badge-repo
            # Only the fresh-init path needs the remote: clone already has it,
            # and adding a duplicate fails the step under set -e.
            git -C badge-repo remote add origin "$url"
          fi
          cp "$RUNNER_TEMP/coverage.svg" badge-repo/
          cd badge-repo
          git config user.name "hordeforge-ci"
          git config user.email "ci@hordeforge.noreply.github.com"
          git add coverage.svg
          git diff --cached --quiet || git commit -m "update coverage badge"
          git push origin badges
